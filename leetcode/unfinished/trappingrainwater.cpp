#include <bits/stdc++.h>
using namespace std;

int trap(vector<int>& height) {
    int count = 0;
    for (int i = 1; i < height.size() - 1; i++) {
        if(height[])
    }
}

int main() {
    vector <int> height = { 0,1,0,2,1,0,1,3,2,1,2,1 };
    cout << trap(height);
}